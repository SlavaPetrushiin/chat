doctype html
html
    head
        title Регистрация
        meta(charset='utf-8')
        meta(name="viewport", content="width=device-width, initial-scale=1, minimum-scale=0.5, maximum-scale=2.0")        
        link(href="/normalize.css" rel="stylesheet" type="text/css")
        link(href="/style.css" rel="stylesheet" type="text/css")
        link(rel="stylesheet" href="/chat.css" type="text/css")        
        script(src="//ajax.googleapis.com/ajax/libs/jquery/3.1.0/jquery.min.js")
        script(src="https://cdnjs.cloudflare.com/ajax/libs/socket.io/2.2.0/socket.io.dev.js")        

    body
        div.authorization
            p(#title)  Авторизация
            form(action="/" class="authorization__form" method="post")
                input(type="text" name="name" placeholder="ФИО").input
                input(type="text" name="nik" placeholder="Ник").input 
                button.authorization__btn Войти
        script(src="/register.js")
        script(src="/chat.js")
    
